  <%# Footer Navigation %>
<nav class="flex justify-around items-center lg:hidden fixed bottom-0 left-0 right-0 px-6 py-3 bg-white shadow-lg border-t border-gray-200 rounded-t-xl ">
  <%# Navigation Links %>
  <%#= link_to root_path, class: "group flex flex-col items-center gap-y-1 p-2 text-xs font-medium #{current_page?(root_path) ? 'bg-gray-100 text-black rounded-lg fill-gray-500' : 'text-gray-500 hover:text-black'}" do %>
    <%#= dashboard_icon %>
<!--    Dashboard-->
  <%# end %>

  <%= link_to dashboard_index_path, class: "group flex flex-col items-center gap-y-1 p-2 text-xs font-medium #{current_page?(dashboard_index_path) ? 'bg-gray-100 text-black rounded-lg fill-gray-500' : 'text-gray-500 hover:text-black'}" do %>
    <%= agent_icon %>
    Agents
  <% end %>

  <%= link_to dashboard_index_path, class: "group flex flex-col items-center gap-y-1 p-2 text-xs font-medium #{current_page?(dashboard_index_path) ? 'bg-gray-100 text-black rounded-lg fill-gray-500' : 'text-gray-500 hover:text-black'}" do %>
    <%= company_icon %>
    Companies
  <% end %>

  <%# Notification button %>
  <%= link_to '#', class: "group relative flex flex-col items-center gap-y-1 p-2 text-xs font-medium #{current_page?('#') ? 'bg-gray-100 text-black rounded-lg fill-gray-500' : 'text-gray-500 hover:text-black'}" do %>
    <%= bell_icon %>
    <!--Notification counts goes in here-->
    <span class="absolute top-1 right-5 flex items-center justify-center">
      <span class="w-2.5 h-2.5 bg-white rounded-full flex items-center justify-center">
        <span class="w-1.5 h-1.5 bg-red-500 rounded-full"></span>
      </span>
    </span>
    Activity
  <% end %>
  <%# More button %>
  <%= link_to '#', class: "group flex flex-col items-center gap-y-1 p-2 text-xs font-medium #{current_page?('#') ? 'bg-gray-100 text-black rounded-lg fill-gray-500' : 'text-gray-500 hover:text-black'}" do %>
    <%= more_doted_icon %>
    More
  <% end %>
</nav>
