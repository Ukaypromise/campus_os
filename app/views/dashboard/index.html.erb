<div class="w-full px-8 py-6">
  <div class="flex items-center justify-between mb-8">
    <div class="flex items-center space-x-8">
      <div class="flex items-center space-x-2">
        <div class="flex justify-center items-center w-6 h-6 bg-slate-950 rounded-lg ring-2 ring-white p-1">
          <%= white_company_icon %>
        </div>
        <h1 class="text-2xl font-semibold">Institution</h1>
      </div>
    </div>
    <!-- Add Company button -->
    <%= render AddButtonComponent.new(
      path: new_institution_path,
      text: "Add Institution",
      icon: :plus_icon
    ) %>
  </div>
</div>
